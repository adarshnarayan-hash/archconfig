# Workspace 1 prefers external
workspace = 1, monitor:HDMI-A-1, default:true

# Fallback rule removed: a second `workspace = 1` rule conflicts with the
# one above and ends up pinning workspace 1 to eDP-1. Hyprland already falls
# back to the only available monitor when HDMI-A-1 is absent.
# workspace = 1, monitor:eDP-1

